name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        run: |
          pip install pytailwindcss
          tailwindcss -i tailwind.styles.css -o src/public/styles.css --minify

      - name: Setup Space CLI
        run: |
          curl -fsSL https://deta.space/assets/space-cli.sh | sh
          export PATH="/home/runner/.detaspace/bin:$PATH"
          space login --with-token ${{ secrets.DETA_TOKEN }}

      - name: Deploy
        run: |
          space push
